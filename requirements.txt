fabric==1.4.2
Flask==0.9
lxml==2.3.5
pep8==1.0.1
pylint==0.25.1
pymongo==2.3
uWSGI==1.2.6
mongoengine==0.7.5
redis==2.6.2
hiredis==0.1.1
requests==0.14.1
BeautifulSoup==3.2.1
boto==2.6.0
Jinja2==2.6
pytz==2012j
icalendar==3.4
python-dateutil==2.1

# Useful for local development, e.g. by tools/devshell.py
ipython==0.13.1

# Used for testing
nose==1.1.2
freezegun==0.1.4
selenium==2.33.0

# Useful for profiling and debugging
Flask-DebugToolbar==0.7.1

# NOTE(jlfwong): Since I *just* started working on this, version might fluctuate
# a lot, so please do not hardcode a version number here
Flask-DebugToolbar-LineProfilerPanel==1.0.2

# For async-processing using Redis. This is currently only used for Sift Science
# testing. For real async we should probably use Celery + RabbitMQ
hotqueue==0.2.7

# For async processing
celery==3.1.6
